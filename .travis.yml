language: python

python:
  - "3.7"
  - "3.8"
  - "3.9"
  - "3.10"

install:
  -pip install -r requirements.txt

script:
  -python -m unittest discover -s Tests

branches:
  only:
    -master

env:
  - PYTHONPATH="${PYTHONPATH}:Package1:Package2:Package